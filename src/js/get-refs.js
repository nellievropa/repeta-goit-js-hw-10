export default function getRefs(params) {
    const refs = {
        cardContainer: document.querySelector('.js-card-container'),
        saerchForm: document.querySelector('.js-search-form'),
   };
   return refs;
}